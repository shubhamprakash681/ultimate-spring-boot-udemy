spring:
  application:
    name: product

  config:
    # if configserver is down then it will fall back to local configs
    import: optional:configserver:http://localhost:8888
#  profiles:
#    active: ${SPRING_PROFILE_ACTIVE}
